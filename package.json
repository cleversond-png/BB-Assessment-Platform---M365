{
  "name": "bb-assessment-platform",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run --prefix frontend build",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
